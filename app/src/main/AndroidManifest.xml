<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wanghui.kotlin">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".ui.view.roundview.TestRoundViewActivity" />
        <activity android:name=".ui.view.textview.TestTextViewActivity" />
        <activity android:name=".ui.view.line.TestLineViewActivity" />
        <activity android:name=".test.TestAssetActivity" />
        <activity android:name=".ui.view.flowlayout.TestFlowLayoutActivity" />
        <activity android:name=".ui.view.views.TestViewsActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".ui.view.state.StateTestActivity" />
        <activity android:name=".ui.Main3Activity" />
        <activity android:name=".ui.layoutmanager.LayoutManager1Activity"></activity>
    </application>

</manifest>